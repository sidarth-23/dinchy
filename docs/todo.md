- potentially adding todo md in the root with checkboxes for features

- make sure organisation and other things works properly with totp and sso also. Check the password based flow also for auth

- mise tasks and deploy scripts is getting bigger. Add in taskfile properly. For certs store, use tmp or the repo and completely clear off the certs folder in deploy. Make mise as source of dependencies. Remove experimental flag.
- analyse the worth of ide tasks, taskfile tasks and the tasks which are there in the codebase natively
- readme is not a place to dump explanation about commands. See the readme in deploy and root and make sure the readme explain the setup and not just the commands in detail
- check the need for a debug task for caddy server and if the debugger can show logs
